{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}

<div class="jumbotron jumbotron-fluid" style=" border-bottom: 2px solid #343a40;">
  <div class="container" style="text-align: center;">
    <p class="lead">
        Nous vous souhaitons la bienvenue sur notre site de vente en ligne de produits paramédicaux et bien-être
à destination des particuliers.

Avec plus de 2000 produits paramédicaux à la vente, GsbPara vous propose à un tarif compétitif un large panel de produits livré rapidement chez vous !</p>
  </div>
</div>


{% include '/base/flash.html.twig' %}
<div class="container">
<div class="card card-body">
  <div class="row">
    <div class="col-sm-4">
  <div style="padding:3px;" class="card card-body">

    <nav class="nav flex-column">
        {% for categorie in categories %}
            <a class="nav-link active" href="{{ path('app_product_category', {'id': categorie.getId() }) }}">{{ categorie.getLibelle() }}</a>
        {% endfor %}
    </nav>

</div>
    </div>
    <div class="col-sm-8">

  <nav aria-label="breadcrumb">
     <ol class="breadcrumb">
       <li class="breadcrumb-item"><a href="#">Accueil</a></li>
       <li class="breadcrumb-item active" aria-current="page">Produits</li>
     </ol>

   </nav>




  <div class="row">

  {% for produit in products %}
          <div class="col-lg-4 col-md-6 mb-4">
            <div class="card h-100">
              <center>
              <a href="{{ path('app_product_show', {'id': produit.id }) }}"><img class="card-img-top" src="{{ produit.illustration }}" style="margin-top: 5px;text-align: center;height: 190px; width: 150px;" alt=""></a>
              </center>
              <div class="card-body">
                <a style="text-decoration:none; color:black;" href="{{ path('app_product_show', {'id': produit.id }) }}"><h5>{{ produit.nom }}</h5></a>
                <p class="card-text">{{ produit.getResume() }}</p>
                <a href="{{ path('app_product_show', {'id': produit.id }) }}" class="btn btn-sm btn-outline-secondary" href="allocine-details.html">Plus de détails</a>
              </div>
              <div class="card-footer">
                <small class="text-muted">&#9733; &#9733; &#9733; &#9733; &#9734;</small>
              </div>
            </div>
          </div>
          
{% endfor %}

          </div>
</div>


</div>
  </div>
</div>



</div>

{% endblock %}
